# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# amplify
.amplify
amplify_outputs*
amplifyconfiguration*

# Local-only dirs. Tailwind v4 auto-scans every non-gitignored file for
# class names, so leaving these unignored injects stray utilities into the
# bundle and makes local builds diverge from CI.
.vite
.claude
